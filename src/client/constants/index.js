import * as users from './users.constants';
import * as auth from './auth.constants';
import * as admins from './admins.constants';

export const { FETCH_USERS, FETCH_USERS_SUCCESS } = users;
export const { FETCH_CURRENT_USER } = auth;
export const { FETCH_ADMINS } = admins;
